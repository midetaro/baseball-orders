@AGENTS.md

See the root `CLAUDE.md` for the Claude Code equivalents of the Codex-specific
mechanics (`.claude/agents` in place of `.codex/agents`, `.claude/skills` in
place of `.agents/skills`) referenced by the repository-wide `AGENTS.md`.
