# 画面の変更概要
status: done

- 全員盗塁、全員バントというボタンを追加する（二回押すと全員オフになる）
- 数字の入力は現状通り1%単位だが、0.1%単位のゼロは常に表示する
- シミュレーションボタンは「打順入力」ラベルの右隣に置く
- 「名前は入力せず、一番〜九番として送信します。」というラベルは不要
- 初期状態から、どの表示ラベルも隠れていないこと
- 出塁率の上限は60%
- 画面の左上には、「打順監督」という名前で表示して。ブラウザのタブも同じ
- 画面から「Baseball Orders / Simulator LINEUP BUILDER 」という文言を消去して。ユーザに表示するサービス名を「打順監督」とします
